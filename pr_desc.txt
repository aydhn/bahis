🎯 **What:** Fully integrated missing extension code usages into the primary pipeline to satisfy the architectural requirements (`TÜM UZANTI KODLAR KULLANILACAK`).

💡 **Why:** Several high-end algorithmic extensions like `AlphaGenerator`, `PhilosophicalRiskEngine`, `AutoTuner`, and `fast_math` were initialized inside the dependency container but were only partially executing or completely ignored inside the main orchestration flow.
- `AlphaGenerator` emitted events that no module subscribed to.
- `PhilosophicalRiskEngine` was injected into the `RiskStage` but its `assess_state` was never called to alter the final stake penalty limits.
- `AutoTuner` was correctly parsing recent results inside `Sentinel` but missing updates for `PortfolioManager`.
- `fast_math` contained heavily optimized `njit` math operations like `fast_entropy` which were not leveraged by the `PortfolioManager` to process large batch optimizations quickly.

✅ **Verification:**
- Verified `fast_entropy` calculation operates perfectly inside `PortfolioManager` logging the portfolio aggregate entropy.
- Assured `AlphaGenerator`'s `alpha_signal` properly creates synthetic opportunities for optimization.
- Assured `PhilosophicalRisk` properly applies penalties using `insight.suggested_kelly_penalty`.
- Executed the full unit test suite `PYTHONPATH=. python3 -m pytest tests/` returning `187 passed in 14.08s` and no pipeline regressions.

✨ **Result:** Maximum utilization of quantitative analysis and code efficiency across the entire codebase.
