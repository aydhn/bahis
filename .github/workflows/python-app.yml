name: Python application

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Kodu Çek
      uses: actions/checkout@v4
      
    - name: Python 3.12 Kur
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        
    - name: Bağımlılıkları Yükle
      run: |
        python -m pip install --upgrade pip
        pip install wheel setuptools "numpy<2.0.0" pytest
        pip install --no-build-isolation iisignature
        export SKLEARN_ALLOW_DEPRECATED_SKLEARN_PACKAGE_INSTALL=True
        if [ -f requirements.txt ]; then pip install -r requirements.txt --use-deprecated=legacy-resolver; fi
        
    - name: Pytest ile Testleri Çalıştır
      run: |
        PYTHONPATH=. pytest
